{
  "name": "realtimeescape-game-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authoritative Colyseus game server for RealTimeEscape. One private team session = one Room instance.",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "dev": "node --watch index.mjs"
  },
  "//": "Pinned to the 0.16 line: colyseus.js (the browser client) has no 0.17 release, and a 0.16 client cannot consume a 0.17 seat reservation — it fails with 'Cannot read properties of undefined (reading name)'. Move both together when the 0.17 client ships.",
  "dependencies": {
    "@colyseus/schema": "^3.0.76",
    "@colyseus/ws-transport": "^0.16.5",
    "colyseus": "^0.16.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "colyseus.js": "^0.16.22"
  },
  "engines": {
    "node": ">=20"
  }
}
